(()=>{let ac=null,master=null,timer=null,next=0,step=0,running=false;const bpm=128,beat=60/bpm/2;const melody=[69,71,72,76,74,72,71,69,67,69,71,72,69,67,64,67,69,72,74,76,74,72,71,69,67,64,67,69,71,69,67,64];const bass=[45,45,41,41,43,43,40,40];function ctx(){if(!ac){ac=new(window.AudioContext||window.webkitAudioContext)();master=ac.createGain();master.gain.value=.12;master.connect(ac.destination)}return ac}function tone(freq,t,d,type='triangle',vol=.12){const A=ctx(),o=A.createOscillator(),g=A.createGain();o.type=type;o.frequency.setValueAtTime(freq,t);g.gain.setValueAtTime(.0001,t);g.gain.exponentialRampToValueAtTime(vol,t+.018);g.gain.exponentialRampToValueAtTime(.0001,t+d);o.connect(g);g.connect(master);o.start(t);o.stop(t+d+.03)}function hz(n){return 440*Math.pow(2,(n-69)/12)}function schedule(){if(!running)return;const A=ctx();while(next<A.currentTime+.18){let i=step%melody.length,n=melody[i];tone(hz(n),next,beat*.85,'triangle',.10);if(i%2===0)tone(hz(n-12),next,beat*.55,'sine',.035);if(i%4===0){let b=bass[Math.floor(i/4)%bass.length];tone(hz(b),next,beat*3.6,'sawtooth',.035);tone(hz(b+7),next,beat*3.3,'triangle',.022)}if(i%8===0){tone(hz(57),next,beat*.22,'square',.018)}next+=beat;step++}timer=setTimeout(schedule,60)}async function start(){const A=ctx();if(A.state==='suspended')await A.resume();if(running)return;running=true;next=A.currentTime+.04;schedule()}async function pause(){if(!ac)return;running=false;clearTimeout(timer);if(ac.state==='running')await ac.suspend()}async function resume(){if(!ac){start();return}if(ac.state==='suspended')await ac.resume();if(!running){running=true;next=ac.currentTime+.04;schedule()}}document.addEventListener('DOMContentLoaded',()=>{const s=document.querySelector('#start'),r=document.querySelector('#retry'),st=document.querySelector('#stop'),re=document.querySelector('#resume');if(s)s.addEventListener('click',start,{capture:true});if(r)r.addEventListener('click',start,{capture:true});if(st)st.addEventListener('click',pause,{capture:true});if(re)re.addEventListener('click',resume,{capture:true});});window.GameBGM={start,pause,resume};})();